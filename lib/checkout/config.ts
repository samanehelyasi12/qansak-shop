/**
 * پیکربندی موقتی سمت فرانت‌اند.
 *
 * ⚠️ این مقدار فقط برای فاز فرانت‌اند است و منبع حقیقت محسوب نمی‌شود.
 *    بعد از اتصال بک‌اند جنگو، هزینه ارسال باید از سمت سرور خوانده شود
 *    و این ثابت حذف یا فقط به‌عنوان fallback استفاده گردد.
 */

/** هزینه ارسال موقتی (تومان) — در کل مسیر خرید یکسان استفاده می‌شود. */
export const SHIPPING_COST: number = 30000;
